# GitHub Actions 云端定时部署说明（电脑关机也能推送）

> 原理：把脚本放到 GitHub Actions 上，由 GitHub 的服务器按工作日定时替你跑
> （情绪→竞价→尾盘→涨停池），结果照旧推送到你手机微信（Server酱）。
> 你的电脑关机、人在哪都无所谓。

## 目录结构（部署包 = gh_deploy 里的内容）

```
（仓库根目录）
├─ .github/workflows/daily-scan.yml   每日定时任务定义
├─ python/
│  ├─ requirements.txt
│  ├─ config.py         已脱敏：密钥留空，由 Secret 注入
│  ├─ gh_pipeline.py    按北京时间选择本次该跑哪个脚本
│  └─ 其余脚本(providers/common/auction/tail/mood/build/notify/selfcheck)
└─ .gitignore
```

## 第一步：上传到你的仓库

仓库：`https://github.com/xieji9263-sudo/xiangshu`
把 gh_deploy 里的内容放到**仓库根目录**（若仓库已有其它文件，可放入子目录，但需同步改
`daily-scan.yml` 里的 `working-directory` 与各路径）。

方式 A（命令行，需装了 git 的电脑）：
```bash
git clone https://github.com/xieji9263-sudo/xiangshu.git
# 把 gh_deploy 内的 .github、python、.gitignore 复制到克隆下来的目录根
cd xiangshu
git add -A
git commit -m "add daily stock scan"
git push
```

方式 B（不装 git，网页上传）：
1. 打开仓库页 → Add file → Upload files；
2. 把 `.github\workflows\daily-scan.yml`、`python\` 下全部文件逐个拖入（或用 GitHub Desktop）。
> 注：网页方式看不到隐藏文件夹 `.github`，请用方式 A 或先在本地把 .github 拖入——最简单仍是 A。

## 第二步：添加密钥（不放进代码）

仓库 → Settings → Secrets and variables → Actions → New repository secret：
- Name：`NOTIFY_SERVERCHAN_KEY`
- Secret：你的 Server酱 SENDKEY（SCT 开头那串）

> 这样仓库即使公开，密钥也只存 GitHub 加密存储，不会出现在代码/日志里。

## 第三步：手动验证一次

仓库 → Actions → 左侧 `daily-stock-scan` → 右侧 **Run workflow** → 点绿色按钮。
约 3~6 分钟后刷新页面看日志（跑的是"情绪+涨停池+尾盘复盘"），同时**手机会收到推送**。

## 第四步：自动运行（无需任何操作）

| 北京时间 | 任务 | 说明 |
| :--- | :--- | :--- |
| 08:50 | 情绪报告 | 当天定调 |
| 09:26 | 竞价扫描 | 先自动重建上一交易日涨停池（Actions 每次全新环境）再扫描 |
| 14:40 | 尾盘一夜持股 | 含市场情绪+建议+加粗候选 |
| 15:05 | 当日涨停池 | 为次日准备 |

周一到周五执行（周六日休息）。Actions 免费额度足够这类轻量任务。

## 排错速查
- 没收到推送：看仓库 Actions 该次运行日志（红色步骤点开），常见：
  1) Secret 未添加或名字打错（日志出现 "未配置 serverchan_sendkey"）；
  2) 数据源在 GitHub 服务器地域被限制 → 把 `python/config.py` 的
     `PROVIDER['spot']` 在 `tencent`/`em` 间切换再试；
  3) 涨停池接口当日返回空（节假日/非交易日）→ 属正常，那日竞价会提示无候选。
- 时间对不上：Actions 页面显示的是 UTC（北京时间 -8h）；调度若排队延迟几分钟，脚本窗口会自动容错。
- 想临时改时间：编辑 `daily-scan.yml` 的 `cron`（UTC）。

## 重要
- 本部署包已**去除本机密钥**；请勿把 SENDKEY 写进代码再提交。
- 内容仅供学习研究，不构成投资建议；数据源为免费接口，请以实际运行为准并严格止损。
